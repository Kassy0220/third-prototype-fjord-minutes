<h1><%= minute.title %></h1>

<div id="minute_content">
  <div>
    <div>プレビュー</div>
    <div id="markdown_preview"></div>
  </div>

  <div>
    <div>Markdown</div>
    <pre id="raw_markdown">
<%= MinuteTemplate.build(minute, attendees_list(minute.day_attendees), attendees_list(minute.night_attendees), topics_list(minute.topics), format_date(minute.next_date), absentees_list(minute.absentees)) %>
    </pre>
  </div>
</div>
