<h1>
  <!--GitHub認証を行っていないユーザーデータはlogin_nameを表示しておく-->
  <%= "#{@member.name ? @member.name : @member.login_name}のページ" %>
</h1>

<!--GitHub認証を行っていないユーザーデータが存在するため、if条件をつけておく-->
<% if @member.avatar_url %>
  <%= image_tag(@member.avatar_url, size: '100x100', alt: 'ユーザーのアイコン画像') %>
<% end %>
